apiVersion: v1
kind: Service
metadata:
  name: devops-challenge-service
  labels:
    app: devops-challenge-app
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: devops-challenge-app
